# Committed on purpose: these are defaults, never secrets.
# Machine-specific values go in .env.local, which is gitignored.
ENVIRONMENT=development
LOG_LEVEL=DEBUG
VERBOSE_ERRORS=true
CACHE_SECONDS=0
