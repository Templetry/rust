# TemplateApp

Rust CLI generated with [Templetry](https://github.com/Templetry): clap derive parsing, lib + bin split, unit tests.

```sh
cargo run -- --name you
cargo test
cargo build --release      # target/release/template-app
```
